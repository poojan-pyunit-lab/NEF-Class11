#include <stdio.h>

int main() {
    float x, y;
    printf("Enter value of x: ");
    scanf("%f", &x);
    y = 2*x + 5;  // Example formula, you can change as per your book
    printf("y = %.2f\n", y);
    return 0;
}
